import { Typography, Box } from "@mui/material";

export const RoomsHeader = () => {
  return (
    <Box sx={{ mb: 2 }}>
      <Typography variant="h6" fontWeight={500}>
        Список комнат
      </Typography>
    </Box>
  );
};

